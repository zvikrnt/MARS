# Paper Tables

This directory contains paper-ready tables generated from experiment outputs.

Run:

```bash
python scripts/make_tables.py --results results/reproduce --out paper_tables
```

`table_hyperparameters.tex` contains the tuned MARS-TimeBudget negative-marking configuration.
